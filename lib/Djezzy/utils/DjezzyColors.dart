import 'package:flutter/material.dart';
import 'dart:ui';

// Color ColorPrimary = new Color.fromRGBO(250, 252, 255, 0);
// Color ColorPrimary2 = new Color(0xFF4286f4); 0xFF -> the opacity (FF for opaque) 4286f4 -> the hex-color

const colorPrimary = Color(0XFFfafcff); // the grey of background
const colorPrimary_light = Color(0XFF7e818a); // the light grey of text
const colorPrimary_dark = Color(0XFF383b49); // the dark grey of text
const colorAccent = Color(0XFFff3567); // the pink
const colorAccent2 = Color(0XFFff6066); // the pink
const colorAccentOrng = Color(0XFFff6466); // the orng
const colorAccentGreen = Color(0XFF3f9030); // the green
